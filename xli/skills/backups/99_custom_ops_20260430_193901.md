# Custom User Skills

## GitHub CLI
<run>gh repo list</run>
<run>gh issue list</run>
<run>gh pr list</run>

## Docker
<run>docker ps -a</run>
<run>docker images</run>
<run>docker build -t image .</run>
<run>docker run -it image /bin/bash</run>

## PostgreSQL
<run>psql -U user -d database -c "SELECT * FROM table"</run>

## Redis
<run>redis-cli KEYS "*"</run>

## Добавь свои навыки ниже
